<Window x:Class="ImprovisedEosl.Spike.SyncModal.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Improvised EOSL"
        Icon="Assets/App.ico"
        Width="1100"
        Height="760"
        AllowDrop="True"
        PreviewDragOver="Window_PreviewDragOver"
        PreviewDrop="Window_PreviewDrop">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition x:Name="DiagnosticsRow" Height="0" />
        </Grid.RowDefinitions>

        <Grid x:Name="PrimaryToolbar" Grid.Row="0" Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition x:Name="BrowserCommandRow" Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" MinWidth="120" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button x:Name="BackButton"
                    Grid.Row="0"
                    Grid.Column="0"
                    Margin="0,0,4,0"
                    Style="{StaticResource BrowserCommandButtonStyle}"
                    ToolTip="Back"
                    AutomationProperties.Name="Back"
                    Click="Back_Click"
                    >
                <Path Width="20" Height="20" Stretch="Uniform"
                      Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                      StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                      Data="{StaticResource BackIconGeometry}" />
            </Button>
            <Button x:Name="ForwardButton"
                    Grid.Row="0"
                    Grid.Column="1"
                    Margin="0,0,4,0"
                    Style="{StaticResource BrowserCommandButtonStyle}"
                    ToolTip="Forward"
                    AutomationProperties.Name="Forward"
                    Click="Forward_Click"
                    >
                <Path Width="20" Height="20" Stretch="Uniform"
                      Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                      StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                      Data="{StaticResource ForwardIconGeometry}" />
            </Button>
            <Button x:Name="ReloadButton"
                    Grid.Row="0"
                    Grid.Column="2"
                    Margin="0,0,4,0"
                    Style="{StaticResource BrowserCommandButtonStyle}"
                    ToolTip="Reload"
                    AutomationProperties.Name="Reload"
                    Click="Reload_Click"
                    >
                <Path Width="20" Height="20" Stretch="Uniform"
                      Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                      StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                      Data="{StaticResource ReloadIconGeometry}" />
            </Button>
            <Grid x:Name="AddressEntryHost"
                  Grid.Row="0"
                  Grid.Column="3"
                  Margin="0,0,4,0">
                <TextBox x:Name="AddressBox"
                     Padding="6,4,138,4"
                     AutomationProperties.AutomationId="AddressBox"
                     AutomationProperties.Name="Address"
                     GotKeyboardFocus="AddressBox_GotKeyboardFocus"
                     PreviewMouseLeftButtonDown="AddressBox_PreviewMouseLeftButtonDown"
                     KeyDown="AddressBox_KeyDown" />
                <Button x:Name="CompatibilityStatusButton"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Margin="0,2,3,2"
                    Padding="6,2"
                    MinWidth="112"
                    MaxWidth="132"
                    MinHeight="24"
                    Style="{StaticResource LabeledCommandButtonStyle}"
                    Click="CompatibilityStatus_Click">
                <StackPanel Orientation="Horizontal">
                    <Path x:Name="CompatibilityStatusIconPath"
                          Width="14" Height="14" Margin="0,0,4,0" Stretch="Uniform"
                          Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                          StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                          StrokeLineJoin="Round"
                          Data="{StaticResource CompatibilityOperationalIconGeometry}" />
                    <TextBlock x:Name="CompatibilityStatusText"
                               VerticalAlignment="Center"
                               FontSize="11"
                               Text="互換: 確認中"
                               TextTrimming="CharacterEllipsis" />
                </StackPanel>
                </Button>
            </Grid>
            <Button x:Name="ApplicationSettingsButton"
                    Grid.Row="0"
                    Grid.Column="4"
                    Margin="0,0,4,0"
                    Style="{StaticResource BrowserCommandButtonStyle}"
                    Click="ShellHub_Click">
                <Path Width="20" Height="20" Stretch="Uniform"
                      Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                      StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                      Data="{StaticResource SettingsIconGeometry}" />
            </Button>
        </Grid>

        <Grid x:Name="ParentWebViewHost" Grid.Row="1">
            <wv2:WebView2 x:Name="ParentWebView"
                          AllowExternalDrop="False"
                          Margin="8" />
        </Grid>

        <TextBox x:Name="LogBox"
                 Grid.Row="2"
                 Margin="8"
                 Visibility="Collapsed"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 TextWrapping="NoWrap" />
    </Grid>
</Window>
