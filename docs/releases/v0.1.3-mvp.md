# v0.1.3-mvp

This experimental Windows package separates normal browser startup from the internal
`showModalDialog` validation page.

## Changes

- Normal startup now opens a neutral local home page with web search and shortcuts.
- The `showModalDialog` test harness remains available through the `互換機能テスト` shortcut.
- Automatic validation still opens the test harness directly.
- A selected compatibility profile still takes precedence and opens its configured `startUrl`.
- The visible application title is now `Improvised EOSL` rather than the synchronization-spike title.
- User documentation now states that `file:` parent pages have opaque origins and that dialog
  navigation remains limited to HTTP(S). `data:`, `file:`, and `javascript:` child URLs are rejected.

## Download and start

1. Download `ImprovisedEosl-0.1.3-mvp-win-x64.zip` from the release assets.
2. Extract the entire ZIP to a writable folder.
3. Double-click `ImprovisedEosl.Spike.SyncModal.exe`.
4. Use the address bar for normal browsing or open `互換機能テスト` from the home page.

The ZIP includes the .NET 8 runtime. Microsoft Edge WebView2 Runtime remains required.

## Validation

- solution build completed with 0 warnings and 0 errors
- all 42 policy tests passed
- all three `--auto` synchronous child-dialog cycles passed after the startup change
- normal startup logged `source=home` and navigated to the local `home.html`

This remains an experimental technical preview for isolated evaluation, not a production
compatibility guarantee or supported Internet Explorer replacement.
